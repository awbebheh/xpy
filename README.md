_G.claimCrateConn = RunService.RenderStepped:Connect(function()
task.wait（2）-2秒循环一次
-- 玩家核心变量（确保功能正常运行）【只加1个必需依赖】
local Player = game.Players.LocalPlayer
local Character = Player.Character or Player.CharacterAdded:Wait()
local RootPart = Character:WaitForChild("HumanoidRootPart")
结束
-新增循环功能模块（mayoto mayou，Tab 1）-1

local ClaimCrateToggle = Tab1:CreateToggle({
-新增循环功能模块（mayoto mayou，Tab 1）-1
    Name = "小远专属免费",
    SizeX = 400,
    SizeY = 1000, -- 微调高度适配新增功能
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "小远工具盒",
        FileName = "配置保存"
    },
    KeySystem = false -- 关闭密钥验证，手机端直接注入
})

Name=“自动领取免费宝箱”
local Tab1 = W:CreateTab("1.功能", nil) -- 你的核心功能Tab

-- 【移动增强模块】（竖向排列第一组）
local MoveSection = Tab1:CreateSection("📱 移动增强")
-- 1. 开启移动增强总开关
如果 R.Flags.Movementtoging
RootPart.CanCollide = not Value
结束
结束
-- 【自动挂机模块】（竖向排列第二组）
local ClaimCrateToggle = Tab1:CreateToggle({
local ClaimCrateToggle = Tab1:CreateToggle({
结束
如果价值
如果 R.Flags.Movementtoging
Humanoid.JumpPower = Value
结束
})
结束
Tab1:CreateSlider({
Humanoid.JumpPower = Value
结束
结束
-- 4. 穿墙功能（竖向紧跟）
Name=“开启穿墙”
CurrentValue = false,
Flag = "Noclip",
回调=函数（值）
如果 R.Flags.Movementtoging
RootPart.CanCollide = not Value
})
结束
Tab1:CreateSlider({
结束
-- 【自动挂机模块】（竖向排列第二组）
local ClaimCrateToggle = Tab1:CreateToggle({
local ClaimCrateToggle = Tab1:CreateToggle({
结束
Name=“自动领取免费宝箱”
如果R.Flags.Movementtogging
Humanoid.JumpPower = Value
结束
结束
})
Humanoid.JumpPower = Value
Tab1:CreateToggle({
结束
结束
-- 4. 穿墙功能（竖向紧跟）
Name=“开启穿墙”
CurrentValue = false,
Flag = "Noclip",
_G.claimCrateConn = RunService.RenderStepped:Connect(function()
回调=函数（值）
})

local ClaimCrateToggle = Tab1:CreateToggle({
结束
Name=“自动领取免费宝箱”
如果 R.Flags.Movementtoging
local FarmS=Tab1:CreateSection（[]you自动挂机）
-- 4. 穿墙功能（竖向紧跟）
local ClaimCrateToggle = Tab1:CreateToggle({
local ClaimCrateToggle = Tab1:CreateToggle({
结束
Name=“自动领取免费宝箱”
如果 R.Flags.Movementtoging
Humanoid.JumpPower = Value
结束
结束
如果 R.Flags.Movementtoging
Name=“自动领取免费宝箱”
CurrentValue = false,
Name=“自动领取免费宝箱”
CurrentValue = false,
Name=“开启穿墙”
                        task.wait(0.2)
local FarmS=Tab1:CreateSection（[]you自动挂机）
结束
if_G.claimCrateConn那么_G.claimCrateConn:Disconnect（）end
回调=函数（值）
local ClaimCrateToggle = Tab1:CreateToggle({
})
local ClaimCrateToggle = Tab1:CreateToggle({
Tab1:CreateSlider({
如果 R.Flags.Movementtoging
如果 R.Flags.Movementtoging
如果 R.Flags.Movementtoging
-- 【移动增强模块】（竖向排列第一组）
结束
结束
如果 R.Flags.Movementtoging
Name=“自动领取免费宝箱”
})

Name=“自动领取免费宝箱”
CurrentValue = false,
结束
结束
_G.claimCrateConn = RunService.RenderStepped:Connect(function()
结束
if_G.claimCrateConn那么_G.claimCrateConn:Disconnect（）end
回调=函数（值）
回调=函数（值）
如果没有价值
_G.claimCrateConn = RunService.RenderStepped:Connect(function()
Humanoid.JumpPower = Value
                pcall(function()
                    game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("acecateer_knit@1.7.1"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("FreeCrateService"):WaitForChild("RF"):WaitForChild("ClaimFreeCrates"):InvokeServer()
                end)
            end)
结束
Humanoid.JumpPower = Value
})
FileName=“配置保存”
Name=“自动领取免费宝箱”
CurrentValue = false,
Name=“自动领取免费宝箱”
如果 R.Flags.Movementtoging
Name=“自动领取免费宝箱”
Humanoid.WalkSpeed = 16
Humanoid.JumpPower = 50
RootPart.CanCollide = true
Humanoid.JumpPower = Value
结束[Humanoid.JumpPower = Value]结束
结束
                    game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("acecateer_knit@1.7.1"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("VendorService"):WaitForChild("RF"):WaitForChild("Purchase"):InvokeServer(unpack(args))
                end)
            end)
_G.claimCrateConn = RunService.RenderStepped:Connect(function()
结束
})
-- 【自动挂机模块】（竖向排列第二组）
回调=函数（值）
